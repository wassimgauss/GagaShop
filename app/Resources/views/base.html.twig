<!DOCTYPE html>
<!--[if lt IE 7 ]> <html class="ie ie6 ie-lt10 ie-lt9 ie-lt8 ie-lt7 no-js" lang="en-US"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie ie7 ie-lt10 ie-lt9 ie-lt8 no-js" lang="en-US"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie ie8 ie-lt10 ie-lt9 no-js" lang="en-US"> <![endif]-->
<!--[if IE 9 ]>    <html class="ie ie9 ie-lt10 no-js" lang="en-US"> <![endif]-->
<html lang="en-US">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Bizertenet!{% endblock %}</title>
        {% block stylesheets %}
            <!-- Load Fonts -->
            <link rel="stylesheet" type="text/css" media="all" href="http://fonts.googleapis.com/css?family=Inconsolata:300,400,500,700|Unica+One:300,400,500,700">
            <!-- All theme style -->
            <link rel="stylesheet" type="text/css" media="all" href="{{ asset('build/css/min.css') }}">

            <link rel="stylesheet" type="text/css" media="all" href="{{ asset('build/css/style.css') }}">
            <!-- Data tables style -->
            <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/v/dt/dt-1.10.16/datatables.min.css"/>
            <style>
                /*!
                 * Start Bootstrap - Business Casual (http://startbootstrap.com/)
                 * Copyright 2013-2016 Start Bootstrap
                 * Licensed under MIT (https://github.com/BlackrockDigital/startbootstrap/blob/gh-pages/LICENSE)
                 */

                body {
                   background: url('{{ asset('build/img/poly-bg.jpg') }}') no-repeat center center fixed;
                    -webkit-background-size: cover;
                    -moz-background-size: cover;
                    background-size: cover;
                    -o-background-size: cover;
                    width:100%;
                    height:100%;
                    background-size: calc(100% + 50px);
                }
                .btn {
                    border-radius: 25px;
                }
                .box {
                    padding: 0px 0px 0px;
                    //background: #FAFAFA;
                    background: rgba(255,255,255,1);
                }
                .effect3
                {
                    position: relative;
                }
                .effect3:before
                {
                    z-index: -1;
                    position: absolute;
                    content: "";
                    bottom: 15px;
                    left: 10px;
                    width: 50%;
                    top: 80%;
                    max-width:300px;
                    background: #777;
                    -webkit-box-shadow: 0 15px 10px #777;
                    -moz-box-shadow: 0 15px 10px #777;
                    box-shadow: 0 15px 10px #777;
                    -webkit-transform: rotate(-3deg);
                    -moz-transform: rotate(-3deg);
                    -o-transform: rotate(-3deg);
                    -ms-transform: rotate(-3deg);
                    transform: rotate(-3deg);
                }
                .navbar {
                    border: none;
                    -webkit-transition: 0.2s;
                    transition: 0.2s;
                }
                .navbar--size--xl-stick {
                    padding: 1.2rem 0;

                }
                .navbar--size--md {
                    padding: 1rem 0;
                }
                .navbar--color--white {
                    background: #fff;
                }
                .navbar--color--ghost {
                   /* background: rgba(53, 59, 72,0.5);*/
                   /* background: rgba(47, 54, 64,0.4);*/
                    background:
                -webkit-linear-gradient(left,#F2F3F5 1%,#F2F3F5 40%,#F2F3F5 52%,rgba(242,243,245,.63) 90%);
                    margin-bottom: 0px;
                    border-bottom: inherit !important;
                    border-right: inherit !important;
                }
                .navbar--color--ghost a {
                    //color: white;
                    //border-left: inherit !important;

                }
                .navbar--color--ghost .navbar-brand,
                .navbar--color--ghost .navbar-text,
                .navbar--color--ghost li a{
                    /*color:white;*/
                }
/*                .sub-navbar ul li{
                    border-right:1px solid #83939C;
                    border-bottom:1px solid #83939C;
                }*/
                .nav-sticky {
                  border-right: inherit !important;

                }
                .gs {
                    box-shadow: 0 14px 28px rgba(0,0,0,0.25), 0 10px 10px rgba(0,0,0,0.22);
                }
            </style>
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
        <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
        <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
        <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
        <![endif]-->
        <script src="https://use.fontawesome.com/c788770a31.js"></script>


        {#{% include "@Google/Analytics/async_universal.html.twig" %}#}

    </head>
    <body id="top-image">

        {% block body %}
            <div class="container">
                <div class="row">

                    <div class="box">

                        {% block navbar %} {% endblock %}

                        {% block slider %} {% endblock %}

                        {% block collection %} {% endblock %}

                        {% block promotion %} {% endblock %}

                        {% block content %} {% endblock %}

                        {% block brands %} {% endblock %}




                        {% block footer %} {% endblock %}


                    </div>
                </div>
            </div>


                     <!-- open/close -->
            <div class="overlay overlay-simplegenie">
                <i class="overlay-close lil-close"></i>
                <div class="container">
                    <div class="row text-center pos-r">
                        <div class="col-md-7 col-xs-11 col-center col-height-center search-form">
                            <form action="#!" method="POST" class="inputs-bg" id="searchform">
                                <strong>Type a sentence or word you want to search for, And press Enter.</strong>
                                <input type="text" name="q" class="form-control" placeholder="Search..">
                            </form>
                        </div>
                    </div>
                </div>
            </div><!-- /.overlay -->
                  <!-- Go to top -->
            <a href="#top" class="go-to-top">
                <i class="lil-angle-double-down"></i>
            </a>

        {% endblock %}
        {% block javascripts %}
            <script type="text/javascript" src="{{ asset('build/js/min.js') }}"></script>
            <script>
                $(".add-to-fav:not(.added)").on("click",function(){
                    if(!$(this).hasClass("added"))
                    {var b=$(".shopping-fav"),c=$(this).parents(".product"),d=c.find("img").eq(0);
                        if(d){var e=d.clone().offset({top:d.offset().top+150,left:d.offset().left+150}).css({opacity:".9",position:"absolute",height:"50px",width:"50px","z-index":"999999","border-radius":"50%"}).appendTo($("body")).animate({top:b.offset().top+12,left:b.offset().left+12,width:75,height:75},1e3,"easeInOutExpo");setTimeout(function(){b.effect("shake",{times:2},400,a(c))},1500),e.animate({width:0,height:0},function(){$(this).detach()})}$(this).addClass("added"),$(this).parent().attr("data-original-title","Added")}})


                'use strict';
                //sub-navbar
                // grabbing the class names from the data attributes
                var navBar = $('.navbar'),
                        data = navBar.data();
                var subnavBar = $('.sub-navbar'),
                        data = navBar.data();
                // booleans used to tame the scroll event listening a little..
                var scrolling = false,
                        scrolledPast = false;

                // transition Into
                function switchInto() {
                    // update `scrolledPast` bool
                    scrolledPast = true;
                    // add/remove CSS classes
                    navBar.removeClass(data.startcolor);
                    navBar.removeClass(data.startsize);
                    navBar.addClass(data.intocolor);
                    navBar.addClass(data.intosize);
                    subnavBar.removeClass(data.startcolor);
                    subnavBar.removeClass(data.startsize);
                    subnavBar.addClass(data.intocolor);
                    subnavBar.addClass(data.intosize);
                    console.log('into transition triggered!');
                };

                // transition Start
                function switchStart() {
                    // update `scrolledPast` bool
                    scrolledPast = false;
                    // add/remove CSS classes
                    navBar.addClass(data.startcolor);
                    navBar.addClass(data.startsize);
                    navBar.removeClass(data.intocolor);
                    navBar.removeClass(data.intosize);
                    subnavBar.addClass(data.startcolor);
                    subnavBar.addClass(data.startsize);
                    subnavBar.removeClass(data.intocolor);
                    subnavBar.removeClass(data.intosize);
                    console.log('start transition triggered!');
                }

                // set `scrolling` to true when user scrolls
                $(window).scroll(function () {
                    return scrolling = true;
                });

                setInterval(function () {
                    // when `scrolling` becomes true...
                    if (scrolling) {
                        // set it back to false
                        scrolling = false;
                        // check scroll position
                        if ($(window).scrollTop() > 100) {
                            // user has scrolled > 100px from top since last check
                            if (!scrolledPast) {
                                switchInto();
                            }
                        } else {
                            // user has scrolled back <= 100px from top since last check
                            if (scrolledPast) {
                                switchStart();
                            }
                        }
                    }
                    // take a breath.. hold event listener from firing for 100ms
                }, 100);

                $(document).ready(function() {
                    var a=$(".nav-sticky").offset().top;
                    $(window).scrollTop();a>70?$(".nav-sticky").addClass("gs"):$(".nav-sticky").removeClass("gs");

                var movementStrength = 25;
                    var height = movementStrength / $(window).height();
                    var width = movementStrength / $(window).width();
                    $("#top-image").mousemove(function(e){
                        var pageX = e.pageX - ($(window).width() / 2);
                        var pageY = e.pageY - ($(window).height() / 2);
                        var newvalueX = width * pageX * -1 - 25;
                        var newvalueY = height * pageY * -1 - 50;
                        $('#top-image').css("background-position", newvalueX+"px     "+newvalueY+"px");
                    });
                });
            </script>
        {% endblock %}
    </body>
</html>
